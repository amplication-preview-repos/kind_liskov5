datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  createdAt DateTime @default(now())
  firstName String?
  id        String   @id @default(cuid())
  lastName  String?
  password  String
  roles     Json
  updatedAt DateTime @updatedAt
  username  String   @unique
}

model BuildingProject {
  adress       Adress?        @relation(fields: [adressId], references: [id])
  adressId     String?
  construction Construction[]
  createdAt    DateTime       @default(now())
  id           String         @id @default(cuid())
  updatedAt    DateTime       @updatedAt
}

model Construction {
  adress             Adress?          @relation(fields: [adressId], references: [id])
  adressId           String?
  buildingProjects   BuildingProject? @relation(fields: [buildingProjectsId], references: [id])
  buildingProjectsId String?
  createdAt          DateTime         @default(now())
  id                 String           @id @default(cuid())
  updatedAt          DateTime         @updatedAt
}

model Adress {
  buildingProjects BuildingProject[]
  city             String?
  constructions    Construction[]
  country          String?
  createdAt        DateTime          @default(now())
  id               String            @id @default(cuid())
  postcode         String?
  state            String?
  streetname       String?
  updatedAt        DateTime          @updatedAt
}
